<!DOCTYPE html>
<html>
  <head>
    <% include partials/template/head.ejs %>
  </head>
  <body id="<%= pageID %>">
    <h1><%= title %></h1>
    <div class="container">
      <div class="row">
        <div class="col-sm-8">
            <% rooms.forEach(function(item) { %>
                 <li>Seats in <%= item.name %> : <%= item.seats %></li>
                <% }); %>
            <% if (pageID == 'roomsDetail') { %>
              <div id="graphcontainer" style="min-width:320px; max-width:700px;margin: 0 auto"></div>
            <% include partials/content/availabilitygraph.ejs %>
            <% } %>

        </div><!-- primary -->
      </div>
    </div><!-- container -->
    <% include partials/template/jsdefault.ejs %>
     <!--<script>
       var socket = io();
       socket.on('message', function(data){console.log('kakfa'+data);});
    </script>-->
  </body>
</html>